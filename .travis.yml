language: php

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

php:
  - 7.0
  - 7.1
  - nightly

before_script:
  - composer install
  - git config --global user.name travis-ci
  - git config --global user.email travis@example.com

script:
  - ./bin/phpchunkit run --exclude-group=functional --verbose --debug
  - ./bin/phpchunkit run --group=functional --sandbox --create-dbs --verbose --debug

git:
  depth: 1

notifications:
  irc:
    channels:
      - "irc.freenode.net#phpchunkit"
    on_success: change

